# IVR Flow Builder - Visual Guide

## Component Overview

```
┌─────────────────────────────────────────────────────────────────┐
│  IVR Flow Builder                          [Add Node] [Save Flow]│
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐                                               │
│  │ Play Audio   │  ──────────┐                                  │
│  │ (Green)      │             │                                  │
│  │ welcome.mp3  │             ▼                                  │
│  └──────────────┘      ┌──────────────┐                         │
│                        │ Capture Input│                          │
│                        │ (Blue)       │                          │
│                        │ Valid: 1,2,9 │                          │
│                        └──────────────┘                          │
│                               │                                   │
│                               ▼                                   │
│                        ┌──────────────┐                          │
│                        │ Menu         │                          │
│                        │ (Orange)     │                          │
│                        │ 1: donate    │                          │
│                        │ 9: opt-out   │                          │
│                        └──────────────┘                          │
│                                                                   │
│  [Controls: Zoom, Fit View]        [MiniMap]                    │
└─────────────────────────────────────────────────────────────────┘
```

## Node Types

### 1. Play Audio Node (Green - #4CAF50)
```
┌─────────────────────┐
│ ▶ Play Audio        │
├─────────────────────┤
│ welcome.mp3         │
│ Timeout: 10s        │
└─────────────────────┘
```
**Purpose**: Plays pre-recorded audio to caller
**Configuration**: Audio URL, Timeout (optional)

### 2. Capture Input Node (Blue - #2196F3)
```
┌─────────────────────┐
│ ⌨ Capture Input     │
├─────────────────────┤
│ [1] [2] [3] [9]     │
│ Timeout: 10s        │
└─────────────────────┘
```
**Purpose**: Captures DTMF keypad input
**Configuration**: Valid inputs (comma-separated), Timeout (optional)

### 3. Menu Node (Orange - #FF9800)
```
┌─────────────────────┐
│ ☰ Menu              │
├─────────────────────┤
│ 1: trigger_donation │
│ 2: transfer_agent   │
│ 9: add_to_blacklist │
└─────────────────────┘
```
**Purpose**: Routes based on DTMF input
**Configuration**: DTMF key → Action mappings

### 4. Action Node (Purple - #9C27B0)
```
┌─────────────────────┐
│ ⚡ Action            │
├─────────────────────┤
│ [1: send_sms]       │
│ [9: end_call]       │
└─────────────────────┘
```
**Purpose**: Executes specific actions
**Configuration**: DTMF key → Action mappings

## User Interactions

### Adding a Node
```
1. Click [Add Node]
   ↓
2. Select Node Type
   ┌─────────────────┐
   │ Node Type:      │
   │ [Play Audio ▼]  │
   │                 │
   │ Audio URL:      │
   │ [____________]  │
   │                 │
   │ Timeout:        │
   │ [10_________]   │
   │                 │
   │ [Cancel] [Add]  │
   └─────────────────┘
   ↓
3. Node appears on canvas
```

### Connecting Nodes
```
Node A          Node B
┌─────┐        ┌─────┐
│     │───────▶│     │
└─────┘        └─────┘
  ↑              ↑
  │              │
  └──────────────┘
  Drag from edge to edge
```

### Editing a Node
```
1. Click on node
   ↓
2. Edit Dialog Opens
   ┌──────────────────────────┐
   │ Edit Node: node-123  [×] │
   ├──────────────────────────┤
   │ Audio URL:               │
   │ [welcome.mp3_________]   │
   │                          │
   │ Timeout:                 │
   │ [10__________________]   │
   │                          │
   │ DTMF Actions:            │
   │ ┌──────────────────────┐ │
   │ │ [1] send_sms     [×] │ │
   │ │ [9] end_call     [×] │ │
   │ └──────────────────────┘ │
   │                          │
   │ Add Action:              │
   │ Key: [_] Type: [▼] [Add] │
   │                          │
   │           [Close]        │
   └──────────────────────────┘
```

### Configuring DTMF Actions
```
Menu/Action Node
┌─────────────────────┐
│ Current Actions:    │
│ ┌─────────────────┐ │
│ │ [1] donate  [×] │ │
│ │ [2] agent   [×] │ │
│ │ [9] optout  [×] │ │
│ └─────────────────┘ │
│                     │
│ Add New:            │
│ DTMF: [3]           │
│ Type: [send_sms ▼]  │
│        [Add]        │
└─────────────────────┘
```

## Example Flow: Donation Campaign

```
START
  │
  ▼
┌─────────────────────┐
│ Play Audio          │
│ "Press 1 to donate" │
│ "Press 9 to opt-out"│
└─────────────────────┘
  │
  ▼
┌─────────────────────┐
│ Capture Input       │
│ Valid: 1, 9         │
└─────────────────────┘
  │
  ├──[1]──▶ ┌─────────────────────┐
  │         │ Action: Donation    │
  │         │ - Send SMS with link│
  │         │ - Log conversion    │
  │         └─────────────────────┘
  │
  └──[9]──▶ ┌─────────────────────┐
            │ Action: Opt-out     │
            │ - Add to blacklist  │
            │ - End call          │
            └─────────────────────┘
```

## Validation Errors Display

```
┌─────────────────────────────────────────────────┐
│ ⚠ Validation Errors:                            │
│                                                  │
│ • Flow must contain at least one node           │
│ • Node node-123: Audio URL is required          │
│ • Node node-456: Valid inputs are required      │
│ • Node node-789 is not connected to the flow    │
└─────────────────────────────────────────────────┘
```

## Controls and Features

### Canvas Controls
```
┌─────────────────┐
│ [+] Zoom In     │
│ [-] Zoom Out    │
│ [⊡] Fit View    │
│ [🔒] Lock       │
└─────────────────┘
```

### MiniMap
```
┌─────────────┐
│ ┌─┐         │
│ │ │  ┌─┐    │
│ └─┘  │ │    │
│      └─┘    │
│   ┌─┐       │
│   └─┘       │
└─────────────┘
Overview of entire flow
```

### Background Grid
```
· · · · · · · · · · · ·
· · · · · · · · · · · ·
· · · ┌─────┐ · · · · ·
· · · │Node │ · · · · ·
· · · └─────┘ · · · · ·
· · · · · · · · · · · ·
```

## Action Types

### Available Actions
1. **send_sms**: Send text message to caller
2. **transfer_agent**: Transfer call to live agent
3. **add_to_blacklist**: Add number to DNC list
4. **trigger_donation**: Initiate donation process
5. **end_call**: Terminate the call

### Action Configuration
```
┌──────────────────────────────┐
│ DTMF Key: [1]                │
│                              │
│ Action Type:                 │
│ ┌──────────────────────────┐ │
│ │ send_sms              ▼  │ │
│ │ transfer_agent           │ │
│ │ add_to_blacklist         │ │
│ │ trigger_donation         │ │
│ │ end_call                 │ │
│ └──────────────────────────┘ │
│                              │
│ Parameters: (optional)       │
│ [________________________]   │
│                              │
│          [Add Action]        │
└──────────────────────────────┘
```

## Integration in Campaign Creation

```
Campaign Create Wizard
┌─────────────────────────────────────┐
│ Step 1: Basic Information           │
│ Step 2: Schedule & Time Windows     │
│ Step 3: Configuration ◄─── YOU ARE HERE
│ Step 4: Review                      │
└─────────────────────────────────────┘

Configuration Step:
┌─────────────────────────────────────┐
│ Audio Manager                       │
│ [Upload] [Record] [Phone-in]        │
├─────────────────────────────────────┤
│ IVR Flow Configuration              │
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │   [IVR Flow Builder Canvas]    │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Advanced Settings                   │
│ Max Concurrent Calls: [100]         │
└─────────────────────────────────────┘
```

## Keyboard Shortcuts (Future)

```
Ctrl + Z     : Undo
Ctrl + Y     : Redo
Delete       : Delete selected node
Ctrl + C     : Copy node
Ctrl + V     : Paste node
Ctrl + S     : Save flow
Ctrl + A     : Select all nodes
Escape       : Deselect all
```

## Mobile Responsiveness

```
Desktop (1920x1080)
┌────────────────────────────────────┐
│ Full canvas with all controls      │
│ Side-by-side dialogs               │
└────────────────────────────────────┘

Tablet (768x1024)
┌──────────────────────┐
│ Compact canvas       │
│ Stacked dialogs      │
└──────────────────────┘

Mobile (375x667)
┌──────────────┐
│ Vertical     │
│ scroll       │
│ Full-screen  │
│ dialogs      │
└──────────────┘
```

## Color Scheme

```
Play Audio:     #4CAF50 (Green)    - Growth, Start
Capture Input:  #2196F3 (Blue)     - Input, Data
Menu:           #FF9800 (Orange)   - Choice, Decision
Action:         #9C27B0 (Purple)   - Action, Execute

Background:     #FFFFFF (White)
Grid:           #E0E0E0 (Light Gray)
Edges:          #B0B0B0 (Gray)
Selected:       #1976D2 (Dark Blue)
Error:          #F44336 (Red)
Success:        #4CAF50 (Green)
```

## Data Flow

```
User Input
    ↓
ReactFlow State
    ↓
IVRFlowBuilder Component
    ↓
Validation
    ↓
IVRFlowDefinition
    ↓
Campaign Config
    ↓
Backend API
    ↓
Database
```

## Performance Metrics

```
Initial Load:     < 100ms
Node Creation:    < 50ms
Node Connection:  < 50ms
Validation:       < 100ms
Save Operation:   < 200ms
Re-render:        < 16ms (60fps)
```

## Browser Support

```
✅ Chrome 90+
✅ Firefox 88+
✅ Safari 14+
✅ Edge 90+
❌ IE 11 (not supported)
```
