# Database Configuration for Testing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campaign_test
DB_USER=postgres
DB_PASSWORD=postgres

# Twilio SIP Trunk Configuration
SIP_TRUNK_USERNAME=your_twilio_username
SIP_TRUNK_PASSWORD=your_twilio_password

# Asterisk Configuration
AMI_PASSWORD=asterisk_ami_secure_2023
ARI_PASSWORD=asterisk_ari_secure_2023

# AWS S3 Configuration
S3_AUDIO_BUCKET=your-audio-bucket-name

# Note: For property-based tests, you need a running PostgreSQL instance
# You can use Docker: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres:15
