# Database Configuration for Testing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campaign_test
DB_USER=postgres
DB_PASSWORD=postgres

# Note: For property-based tests, you need a running PostgreSQL instance
# You can use Docker: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres:15
